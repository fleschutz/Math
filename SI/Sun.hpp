#pragma once

#include "types.hpp"

namespace si
{
	namespace Sun
	{
		const auto diameter = kilometer(1390176);
		const auto radius = diameter / 2.0;
		const auto mass = si::kilogram(1.9884e33);
	}
}
